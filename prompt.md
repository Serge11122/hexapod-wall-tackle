review the output folder in ground_truth_terrain folder. there is a visualization of a proposed robot body.
implement python scripts and code that models the robot body, a physics enviornment, and output visualizations modules.
implement setup.py at the project root, and create .venv so that the project can be installed using pip install -e . in the.venv, and folders are auto detected as modules and importable from different scripts and modules as needed.

folders to create:
robot_motion
robot_enviornment
robot_vis

robot_motion will have a hardcoded set of animation frame json poses or joint angles for the robot legs. it will also have the body definitions and implementations and code to model and visualize the robot body, legs, and motion to walk left and right. Research online crab or quadrupedal locomotion, if needed, or simulate the locomotion poses and angles in json files for frames as many frames needed to achieve loopable poses for motion left and motion right. using the body example appearance and components in ground truth terrain output folder pngs. the expected visualization will be 5 fps. so implement the json pose definitions accordingly to represent key frames of locomotion for the robot body as needed to move at that frame rate. implement the needed parameters for modeling the physics of the body, like point contacts of the body, radius around the point for friction calculation, friction coefficients, normal forces, masses, torque, forces and other parameters needed to simulatea simple physics interaction between the robot body and legs and contact points and the physics envrioment. (no self collission in the robot body and legs between it's own components, only between components and enviornment)

robot_enviornment will have a physical 2d simulation enviornment comprised of a rectangualar enclosure, similar to the png in the output folder, where line segments will represent the floor and walls (no ceiling needed), and will be 4x long as it is height. with the line segments for wall and floor having normals pointing inward in the enclosure. no step or terrain variations needed, just simple 3 segment enviornment, left wall, floor, right wall. also implement the physics engine and collision engine to simulate the enviornment. so there's gravity, and friction, and point to line collisions (maybe track before frame, and after frame, and line between point motions, and detect line intersections and collissions, and estimate point velocity to calculate force of collission, or force of pressure for friction etc). use the needed parameters forom the robot_body folder modeling the robot as set of points including body boundary points and leg contact points etc. to simulate collisions, friction for walking etc. add the needed parameters in the robot enviornment for a physics simulation that would have enough modeled to allow for successful locomotion. and keep encapsulation where the robot definitions and params are in the robot folder, and env params and definitions are in the enviornment folder.

robot_vis will use opencv (cv2) python library to render the different components of the robot and enviornment simulations. first implement render_locomotion.py that will output the locomotion frames (defined in robot body, render the animation centered and fit to image size around the robot body, to render the frames of the defined animations). then also implement render_simulation_left_right.py that outputs individual frames and a video for each walk direction. simulate the enviornment at 5fps, and render the video and frames at 5fps, and drop the robot at some height from the center of the enviornment above the floor, so that gravity allows the robot to pull towards floor, and collision engine resolves the body position as the frame calculates the next position, then collission engine corrects the actual position by accounting for collision correction of the body position, and recalculating the correct position and solving iteratively for a correct position based on the physics and colission enginge. use any libraries needed for these if libraries are available for these kind of simulation solvers and install needed pip packages. so that also the forces and frictions are calculated as needed to model walking and rebound bouncing from collissions etc.

output path structure (all outputs live under robot_vis/):
  robot_vis/frames/<artifact>/   — PNG frame sequences, one folder per artifact:
    frames/motion_left/          — locomotion keyframes, walk-left gait
    frames/motion_right/         — locomotion keyframes, walk-right gait
    frames/sim_left/             — simulation frames, robot walking left
    frames/sim_right/            — simulation frames, robot walking right
  robot_vis/vid/<artifact>/      — video outputs, one folder per artifact:
    vid/sim_left/left.gif        — animated GIF, walk-left simulation
    vid/sim_right/right.gif      — animated GIF, walk-right simulation


refine the motion of the robot json frame poses definitions by using the robot vis and render_locomotion.py to render the frames designed in the json poses frame definitions. to ensure the locomotion is sufficient and effective at producing a walking left and right (modes) while in the simulation enviornment to effectively achieve walking motion left to right. 

refine the entire system and all components until valid and successful simulation rendering output frames and videos are working in the robot_vis render_locomotion.py video and frame outputs. review the outputs frames, and iterate and refine the simulation and impleemnted code until the robot successfully walks left and right in its simulation rendering and enviornment 3 body lengths in each direction.

IMPORTANT — no hardcoded body motion: the body's horizontal translation must emerge entirely from causal physics. specifically: the leg joint angles drive foot positions, foot-to-floor contact forces (normal force + friction) react against the environment segments, and the resulting net force/impulse on the robot body is what accelerates it left or right. there must be no direct velocity assignment, no external drive force applied to the body, and no teleportation of body position. the only inputs to the physics engine are (a) gravity, (b) joint torques or prescribed joint-angle trajectories acting through the leg linkage, and (c) contact/friction forces computed from foot-environment penetration and relative velocity. if the leg gait produces no net horizontal ground-reaction force, the body must not move horizontally. locomotion must be a consequence of the simulation, not an assumption built into it.
